<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomeMade Delights - Fresh Homemade Food Delivered</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:wght@700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            overflow-x: hidden;
        }
        
        .font-display {
            font-family: 'Playfair Display', serif;
        }
        
        .hero-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .food-card {
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .food-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
        }
        
        .fade-in {
            animation: fadeIn 0.6s ease-in;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.7);
            animation: fadeIn 0.3s ease;
        }
        
        .modal-content {
            background-color: Blue;
            margin: 5% auto;
            padding: 0;
            border-radius: 20px;
            width: 90%;
            max-width: 600px;
            max-height: 85vh;
            overflow-y: auto;
            animation: slideUp 0.3s ease;
        }
        
        @keyframes slideUp {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        
        .admin-panel {
            display: none;
            position: fixed;
            right: -400px;
            top: 0;
            width: 400px;
            height: 100%;
            background: white;
            box-shadow: -5px 0 20px rgba(0,0,0,0.2);
            transition: right 0.3s ease;
            z-index: 999;
            overflow-y: auto;
        }
        
        .admin-panel.active {
            right: 0;
        }
        
        .floating-admin-btn {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            cursor: pointer;
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
            transition: all 0.3s ease;
            z-index: 998;
        }
        
        .floating-admin-btn:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 30px rgba(102, 126, 234, 0.6);
        }
        
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 15px 25px;
            background: yellow;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.2);
            display: none;
            z-index: 1001;
            animation: slideInRight 0.3s ease;
        }
        
        @keyframes slideInRight {
            from { transform: translateX(400px); }
            to { transform: translateX(0); }
        }
        
        .category-filter {
            display: inline-block;
            padding: 8px 20px;
            margin: 5px;
            border: 2px solid #667eea;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .category-filter:hover, .category-filter.active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
    </style>
</head>
<body class="bg-gray-50">

    <!-- Notification -->
    <div id="notification" class="notification"></div>

    <!-- Navigation -->
    <nav class="bg-white shadow-md fixed w-full top-0 z-50">
        <div class="container mx-auto px-6 py-4">
            <div class="flex justify-between items-center">
                <div class="text-2xl font-display font-bold text-purple-600">
                    🍳 HomeMade Delights
                </div>
                <div class="hidden md:flex space-x-8">
                    <a href="#home" class="text-gray-700 hover:text-purple-600 transition">Home</a>
                    <a href="#menu" class="text-gray-700 hover:text-purple-600 transition">Menu</a>
                    <a href="#about" class="text-gray-700 hover:text-purple-600 transition">About</a>
                    <a href="#contact" class="text-gray-700 hover:text-purple-600 transition">Contact</a>
                </div>
                <button onclick="openOrderModal()" class="btn-primary text-black px-6 py-2 rounded-full font-semibold">
                    Order Now
                </button>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section id="home" class="hero-gradient text-black pt-24 pb-20">
        <div class="container mx-auto px-6">
            <div class="flex flex-col md:flex-row items-center">
                <div class="md:w-1/2 mb-10 md:mb-0 fade-in">
                    <h1 class="font-display text-5xl md:text-6xl font-bold mb-6" id="heroTitle">
                        Delicious Homemade Food, Delivered Fresh
                    </h1>
                    <p class="text-xl mb-8 opacity-90" id="heroDescription">
                        Experience the taste of authentic home-cooked meals made with love and fresh ingredients, right in your neighborhood.
                    </p>
                    <div class="flex space-x-4">
                        <button onclick="document.getElementById('menu').scrollIntoView({behavior: 'smooth'})" class="bg-white text-purple-600 px-8 py-3 rounded-full font-semibold hover:shadow-xl transition">
                            View Menu
                        </button>
                        <button onclick="openOrderModal()" class="border-2 border-white px-8 py-3 rounded-full font-semibold hover:bg-white hover:text-purple-600 transition">
                            Order Now
                        </button>
                    </div>
                </div>
                <div class="md:w-1/2 fade-in">
                    <img src="https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=800" alt="Delicious food" class="rounded-3xl shadow-2xl">
                </div>
            </div>
        </div>
    </section>

    <!-- Features -->
    <section class="py-20 bg-blue">
        <div class="container mx-auto px-6">
            <div class="grid md:grid-cols-3 gap-12">
                <div class="text-center fade-in">
                    <div class="text-5xl mb-4">🌿</div>
                    <h3 class="text-xl font-semibold mb-3">Fresh Ingredients</h3>
                    <p class="text-gray-600">We use only the freshest, locally-sourced ingredients for every dish.</p>
                </div>
                <div class="text-center fade-in">
                    <div class="text-5xl mb-4">👨‍🍳</div>
                    <h3 class="text-xl font-semibold mb-3">Made with Love</h3>
                    <p class="text-gray-600">Every meal is prepared with care and passion, just like home.</p>
                </div>
                <div class="text-center fade-in">
                    <div class="text-5xl mb-4">🚀</div>
                    <h3 class="text-xl font-semibold mb-3">Quick Delivery</h3>
                    <p class="text-gray-600">Fast and reliable delivery to your doorstep in your local area.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Menu Section -->
    <section id="menu" class="py-20 bg-gray-50">
        <div class="container mx-auto px-6">
            <div class="text-center mb-12">
                <h2 class="font-display text-4xl md:text-5xl font-bold mb-4">Our Menu</h2>
                <p class="text-gray-600 text-lg">Explore our delicious homemade offerings</p>
            </div>

            <!-- Category Filter -->
            <div class="text-center mb-12" id="categoryFilter">
                <span class="category-filter active" data-category="all">All</span>
                <span class="category-filter" data-category="main-course">Main Course</span>
                <span class="category-filter" data-category="appetizers">Appetizers</span>
                <span class="category-filter" data-category="desserts">Desserts</span>
                <span class="category-filter" data-category="beverages">Beverages</span>
            </div>

            <!-- Menu Items -->
            <div id="menuItems" class="grid md:grid-cols-3 gap-8">
                <!-- Items will be loaded here dynamically -->
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="py-20 bg-white">
        <div class="container mx-auto px-6">
            <div class="flex flex-col md:flex-row items-center">
                <div class="md:w-1/2 mb-10 md:mb-0">
                    <img src="https://images.unsplash.com/photo-1556910103-1c02745aae4d?w=800" alt="About us" class="rounded-3xl shadow-xl">
                </div>
                <div class="md:w-1/2 md:pl-12">
                    <h2 class="font-display text-4xl font-bold mb-6" id="aboutTitle">About Us</h2>
                    <p class="text-gray-600 text-lg mb-6" id="aboutDescription">
                        Welcome to HomeMade Delights! We're a passionate home chef bringing authentic, home-cooked meals to our local community. Every dish is prepared fresh daily using traditional recipes and quality ingredients.
                    </p>
                    <p class="text-gray-600 text-lg mb-6">
                        Our mission is to share the warmth and comfort of homemade food with busy families and food lovers in our neighborhood. We believe that great food brings people together and creates lasting memories.
                    </p>
                    <div class="flex items-center space-x-4">
                        <div class="text-center">
                            <div class="text-3xl font-bold text-purple-600">500+</div>
                            <div class="text-gray-600">Happy Customers</div>
                        </div>
                        <div class="text-center">
                            <div class="text-3xl font-bold text-purple-600">50+</div>
                            <div class="text-gray-600">Dishes</div>
                        </div>
                        <div class="text-center">
                            <div class="text-3xl font-bold text-purple-600">5⭐</div>
                            <div class="text-gray-600">Rating</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="py-20 bg-gray-50">
        <div class="container mx-auto px-6">
            <div class="text-center mb-12">
                <h2 class="font-display text-4xl font-bold mb-4">Get In Touch</h2>
                <p class="text-gray-600 text-lg">Have questions? We'd love to hear from you!</p>
            </div>
            <div class="max-w-2xl mx-auto bg-white rounded-2xl shadow-xl p-8">
                <form id="contactForm" class="space-y-6">
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Name</label>
                        <input type="text" required class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Email</label>
                        <input type="email" required class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Message</label>
                        <textarea required rows="4" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600"></textarea>
                    </div>
                    <button type="submit" class="btn-primary w-full text-white py-3 rounded-lg font-semibold">
                        Send Message
                    </button>
                </form>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-12">
        <div class="container mx-auto px-6">
            <div class="grid md:grid-cols-3 gap-8">
                <div>
                    <h3 class="font-display text-2xl font-bold mb-4">HomeMade Delights</h3>
                    <p class="text-gray-400">Bringing the taste of home to your table, one meal at a time.</p>
                </div>
                <div>
                    <h4 class="font-semibold text-lg mb-4">Quick Links</h4>
                    <ul class="space-y-2 text-gray-400">
                        <li><a href="#menu" class="hover:text-purple-400 transition">Menu</a></li>
                        <li><a href="#about" class="hover:text-purple-400 transition">About</a></li>
                        <li><a href="#contact" class="hover:text-purple-400 transition">Contact</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-semibold text-lg mb-4">Contact Info</h4>
                    <ul class="space-y-2 text-gray-400">
                        <li id="footerPhone">📞 +1 (555) 123-4567</li>
                        <li id="footerEmail">✉️ hello@homemadedelights.com</li>
                        <li id="footerAddress">📍 123 Main St, Your City</li>
                    </ul>
                </div>
            </div>
            <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
                <p>&copy; 2024 HomeMade Delights. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Order Modal -->
    <div id="orderModal" class="modal">
        <div class="modal-content">
            <div class="p-8">
                <div class="flex justify-between items-center mb-6">
                    <h2 class="font-display text-3xl font-bold">Place Your Order</h2>
                    <button onclick="closeOrderModal()" class="text-gray-500 hover:text-gray-700 text-3xl">&times;</button>
                </div>
                <form id="orderForm" class="space-y-4">
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Your Name *</label>
                        <input type="text" required class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Phone Number *</label>
                        <input type="tel" required class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Delivery Address *</label>
                        <textarea required rows="3" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600"></textarea>
                    </div>
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Select Items *</label>
                        <div id="orderItemsList" class="space-y-2 max-h-60 overflow-y-auto border border-gray-200 rounded-lg p-4">
                            <!-- Items will be populated here -->
                        </div>
                    </div>
                    <div>
                        <label class="block text-gray-700 font-semibold mb-2">Special Instructions</label>
                        <textarea rows="2" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600" placeholder="Any dietary restrictions or special requests?"></textarea>
                    </div>
                    <button type="submit" class="btn-primary w-full text-white py-3 rounded-lg font-semibold">
                        Confirm Order
                    </button>
                </form>
            </div>
        </div>
    </div>

    <!-- Admin Panel -->
    <div id="adminPanel" class="admin-panel">
        <div class="p-6">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold">Admin Panel</h2>
                <button onclick="toggleAdminPanel()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
            </div>

            <!-- Tabs -->
            <div class="flex border-b mb-6">
                <button onclick="switchAdminTab('content')" class="admin-tab active px-4 py-2 border-b-2 border-purple-600 font-semibold">Content</button>
                <button onclick="switchAdminTab('menu')" class="admin-tab px-4 py-2 font-semibold text-gray-600">Menu Items</button>
                <button onclick="switchAdminTab('contact')" class="admin-tab px-4 py-2 font-semibold text-gray-600">Contact Info</button>
            </div>

            <!-- Content Tab -->
            <div id="contentTab" class="admin-tab-content">
                <h3 class="text-lg font-semibold mb-4">Update Homepage Content</h3>
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-semibold mb-2">Hero Title</label>
                        <input type="text" id="adminHeroTitle" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Hero Description</label>
                        <textarea id="adminHeroDesc" rows="3" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600"></textarea>
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">About Title</label>
                        <input type="text" id="adminAboutTitle" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">About Description</label>
                        <textarea id="adminAboutDesc" rows="4" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600"></textarea>
                    </div>
                    <button onclick="updateContent()" class="w-full bg-purple-600 text-white py-2 rounded-lg font-semibold hover:bg-purple-700 transition">
                        Update Content
                    </button>
                </div>
            </div>

            <!-- Menu Tab -->
            <div id="menuTab" class="admin-tab-content" style="display:none;">
                <h3 class="text-lg font-semibold mb-4">Add/Edit Menu Items</h3>
                <form id="addMenuForm" class="space-y-4 mb-6">
                    <div>
                        <label class="block text-sm font-semibold mb-2">Item Name</label>
                        <input type="text" id="menuItemName" required class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Description</label>
                        <textarea id="menuItemDesc" required rows="2" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600"></textarea>
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Price ($)</label>
                        <input type="number" id="menuItemPrice" required step="0.01" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Category</label>
                        <select id="menuItemCategory" required class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                            <option value="main-course">Main Course</option>
                            <option value="appetizers">Appetizers</option>
                            <option value="desserts">Desserts</option>
                            <option value="beverages">Beverages</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Image URL</label>
                        <input type="text" id="menuItemImage" required class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <button type="submit" class="w-full bg-purple-600 text-white py-2 rounded-lg font-semibold hover:bg-purple-700 transition">
                        Add Item
                    </button>
                </form>
                <div class="border-t pt-4">
                    <h4 class="font-semibold mb-3">Current Menu Items</h4>
                    <div id="adminMenuList" class="space-y-2 max-h-96 overflow-y-auto">
                        <!-- Menu items will be listed here -->
                    </div>
                </div>
            </div>

            <!-- Contact Tab -->
            <div id="contactTab" class="admin-tab-content" style="display:none;">
                <h3 class="text-lg font-semibold mb-4">Update Contact Information</h3>
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-semibold mb-2">Phone Number</label>
                        <input type="text" id="adminPhone" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Email Address</label>
                        <input type="email" id="adminEmail" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold mb-2">Address</label>
                        <input type="text" id="adminAddress" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600">
                    </div>
                    <button onclick="updateContactInfo()" class="w-full bg-purple-600 text-white py-2 rounded-lg font-semibold hover:bg-purple-700 transition">
                        Update Contact Info
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Floating Admin Button -->
    <div class="floating-admin-btn" onclick="toggleAdminPanel()" title="Admin Panel">
        ⚙️
    </div>

    <script>
        // Default menu items
        const defaultMenuItems = [
            {
                id: 1,
                name: "Homestyle Butter Chicken",
                description: "Tender chicken in rich, creamy tomato sauce with aromatic spices",
                price: 12.99,
                category: "main-course",
                image: "https://images.unsplash.com/photo-1603894584373-5ac82b2ae398?w=400"
            },
            {
                id: 2,
                name: "Fresh Garden Salad",
                description: "Mixed greens with seasonal vegetables and homemade dressing",
                price: 6.99,
                category: "appetizers",
                image: "https://images.unsplash.com/photo-1512621776951-a57141f2eefd?w=400"
            },
            {
                id: 3,
                name: "Classic Chocolate Cake",
                description: "Rich, moist chocolate cake with smooth chocolate frosting",
                price: 8.99,
                category: "desserts",
                image: "https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=400"
            },
            {
                id: 4,
                name: "Vegetable Biryani",
                description: "Fragrant basmati rice with mixed vegetables and aromatic spices",
                price: 10.99,
                category: "main-course",
                image: "https://images.unsplash.com/photo-1563379091339-03b21ab4a4f8?w=400"
            },
            {
                id: 5,
                name: "Crispy Spring Rolls",
                description: "Golden fried rolls filled with fresh vegetables",
                price: 5.99,
                category: "appetizers",
                image: "https://images.unsplash.com/photo-1582878826629-29b7ad1cdc43?w=400"
            },
            {
                id: 6,
                name: "Fresh Lemonade",
                description: "Refreshing homemade lemonade with mint",
                price: 3.99,
                category: "beverages",
                image: "https://images.unsplash.com/photo-1523677011781-c91d1bbe2f0b?w=400"
            }
        ];

        // Initialize localStorage with default data if empty
        function initializeData() {
            if (!localStorage.getItem('menuItems')) {
                localStorage.setItem('menuItems', JSON.stringify(defaultMenuItems));
            }
            if (!localStorage.getItem('contactInfo')) {
                localStorage.setItem('contactInfo', JSON.stringify({
                    phone: '+1 (519)991-4920',
                    email: 'parthp9198@gmail.com',
                    address: '127 Applewood crescent, Guelph'
                }));
            }
        }

        // Load menu items
        function loadMenuItems(category = 'all') {
            const menuItems = JSON.parse(localStorage.getItem('menuItems') || '[]');
            const container = document.getElementById('menuItems');
            
            const filteredItems = category === 'all' 
                ? menuItems 
                : menuItems.filter(item => item.category === category);
            
            container.innerHTML = filteredItems.map(item => `
                <div class="food-card bg-white rounded-2xl shadow-lg overflow-hidden fade-in" data-category="${item.category}">
                    <img src="${item.image}" alt="${item.name}" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h3 class="text-xl font-semibold mb-2">${item.name}</h3>
                        <p class="text-gray-600 mb-4">${item.description}</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-purple-600">$${item.price.toFixed(2)}</span>
                            <button onclick="addToOrder('${item.name}', ${item.price})" class="bg-purple-600 text-white px-4 py-2 rounded-lg hover:bg-purple-700 transition">
                                Add to Order
                            </button>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        // Category filter
        document.getElementById('categoryFilter').addEventListener('click', function(e) {
            if (e.target.classList.contains('category-filter')) {
                document.querySelectorAll('.category-filter').forEach(btn => btn.classList.remove('active'));
                e.target.classList.add('active');
                loadMenuItems(e.target.dataset.category);
            }
        });

        // Order modal functions
        function openOrderModal() {
            const menuItems = JSON.parse(localStorage.getItem('menuItems') || '[]');
            const orderItemsList = document.getElementById('orderItemsList');
            
            orderItemsList.innerHTML = menuItems.map(item => `
                <label class="flex items-center space-x-3 p-2 hover:bg-gray-50 rounded cursor-pointer">
                    <input type="checkbox" name="orderItem" value="${item.name}" data-price="${item.price}" class="w-4 h-4">
                    <span class="flex-1">${item.name}</span>
                    <span class="font-semibold text-purple-600">$${item.price.toFixed(2)}</span>
                </label>
            `).join('');
            
            document.getElementById('orderModal').style.display = 'block';
        }

        function closeOrderModal() {
            document.getElementById('orderModal').style.display = 'none';
        }

        function addToOrder(itemName, price) {
            openOrderModal();
            setTimeout(() => {
                const checkbox = document.querySelector(`input[value="${itemName}"]`);
                if (checkbox) checkbox.checked = true;
            }, 100);
        }

        // Handle order form submission
        document.getElementById('orderForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const selectedItems = Array.from(document.querySelectorAll('input[name="orderItem"]:checked'))
                .map(cb => cb.value);
            
            if (selectedItems.length === 0) {
                showNotification('Please select at least one item!', 'error');
                return;
            }
            
            showNotification('Order placed successfully! We\'ll contact you shortly.', 'success');
            closeOrderModal();
            this.reset();
        });

        // Handle contact form submission
        document.getElementById('contactForm').addEventListener('submit', function(e) {
            e.preventDefault();
            showNotification('Message sent successfully! We\'ll get back to you soon.', 'success');
            this.reset();
        });

        // Admin panel functions
        function toggleAdminPanel() {
            const panel = document.getElementById('adminPanel');
            panel.style.display = panel.style.display === 'none' ? 'block' : 'none';
            if (panel.style.display === 'block') {
                panel.classList.add('active');
                loadAdminData();
            } else {
                panel.classList.remove('active');
            }
        }

        function switchAdminTab(tabName) {
            document.querySelectorAll('.admin-tab').forEach(tab => {
                tab.classList.remove('active', 'border-purple-600');
                tab.classList.add('text-gray-600');
            });
            document.querySelectorAll('.admin-tab-content').forEach(content => {
                content.style.display = 'none';
            });
            
            event.target.classList.add('active', 'border-purple-600');
            event.target.classList.remove('text-gray-600');
            document.getElementById(tabName + 'Tab').style.display = 'block';
        }

        function loadAdminData() {
            // Load content data
            document.getElementById('adminHeroTitle').value = document.getElementById('heroTitle').innerText;
            document.getElementById('adminHeroDesc').value = document.getElementById('heroDescription').innerText;
            document.getElementById('adminAboutTitle').value = document.getElementById('aboutTitle').innerText;
            document.getElementById('adminAboutDesc').value = document.getElementById('aboutDescription').innerText;
            
            // Load contact data
            const contactInfo = JSON.parse(localStorage.getItem('contactInfo') || '{}');
            document.getElementById('adminPhone').value = contactInfo.phone || '';
            document.getElementById('adminEmail').value = contactInfo.email || '';
            document.getElementById('adminAddress').value = contactInfo.address || '';
            
            // Load menu items list
            loadAdminMenuList();
        }

        function updateContent() {
            document.getElementById('heroTitle').innerText = document.getElementById('adminHeroTitle').value;
            document.getElementById('heroDescription').innerText = document.getElementById('adminHeroDesc').value;
            document.getElementById('aboutTitle').innerText = document.getElementById('adminAboutTitle').value;
            document.getElementById('aboutDescription').innerText = document.getElementById('adminAboutDesc').value;
            
            showNotification('Content updated successfully!', 'success');
        }

        function updateContactInfo() {
            const contactInfo = {
                phone: document.getElementById('adminPhone').value,
                email: document.getElementById('adminEmail').value,
                address: document.getElementById('adminAddress').value
            };
            
            localStorage.setItem('contactInfo', JSON.stringify(contactInfo));
            
            document.getElementById('footerPhone').innerText = '📞 ' + contactInfo.phone;
            document.getElementById('footerEmail').innerText = '✉️ ' + contactInfo.email;
            document.getElementById('footerAddress').innerText = '📍 ' + contactInfo.address;
            
            showNotification('Contact information updated!', 'success');
        }

        // Add menu item
        document.getElementById('addMenuForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const menuItems = JSON.parse(localStorage.getItem('menuItems') || '[]');
            const newItem = {
                id: Date.now(),
                name: document.getElementById('menuItemName').value,
                description: document.getElementById('menuItemDesc').value,
                price: parseFloat(document.getElementById('menuItemPrice').value),
                category: document.getElementById('menuItemCategory').value,
                image: document.getElementById('menuItemImage').value
            };
            
            menuItems.push(newItem);
            localStorage.setItem('menuItems', JSON.stringify(menuItems));
            
            loadMenuItems();
            loadAdminMenuList();
            this.reset();
            showNotification('Menu item added successfully!', 'success');
        });

        function loadAdminMenuList() {
            const menuItems = JSON.parse(localStorage.getItem('menuItems') || '[]');
            const container = document.getElementById('adminMenuList');
            
            container.innerHTML = menuItems.map(item => `
                <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                    <div>
                        <div class="font-semibold">${item.name}</div>
                        <div class="text-sm text-gray-600">$${item.price.toFixed(2)} - ${item.category}</div>
                    </div>
                    <button onclick="deleteMenuItem(${item.id})" class="text-red-600 hover:text-red-700 font-semibold">
                        Delete
                    </button>
                </div>
            `).join('');
        }

        function deleteMenuItem(id) {
            if (confirm('Are you sure you want to delete this item?')) {
                let menuItems = JSON.parse(localStorage.getItem('menuItems') || '[]');
                menuItems = menuItems.filter(item => item.id !== id);
                localStorage.setItem('menuItems', JSON.stringify(menuItems));
                
                loadMenuItems();
                loadAdminMenuList();
                showNotification('Menu item deleted!', 'success');
            }
        }

        // Notification system
        function showNotification(message, type = 'success') {
            const notification = document.getElementById('notification');
            notification.innerHTML = `
                <div class="flex items-center space-x-3">
                    <div class="text-2xl">${type === 'success' ? '✓' : '⚠️'}</div>
                    <div class="font-semibold">${message}</div>
                </div>
            `;
            notification.style.display = 'block';
            notification.style.background = type === 'success' ? '#10b981' : '#ef4444';
            notification.style.color = 'white';
            
            setTimeout(() => {
                notification.style.display = 'none';
            }, 3000);
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('orderModal');
            if (event.target === modal) {
                closeOrderModal();
            }
        }

        // Initialize on page load
        window.addEventListener('DOMContentLoaded', function() {
            initializeData();
            loadMenuItems();
            
            // Load contact info
            const contactInfo = JSON.parse(localStorage.getItem('contactInfo') || '{}');
            if (contactInfo.phone) {
                document.getElementById('footerPhone').innerText = '📞 ' + contactInfo.phone;
                document.getElementById('footerEmail').innerText = '✉️ ' + contactInfo.email;
                document.getElementById('footerAddress').innerText = '📍 ' + contactInfo.address;
            }
        });
    </script>

</body>
</html>
